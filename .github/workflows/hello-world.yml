name: hello-world
on: workflow_dispatch
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          pwd
          ls
          cd
          pwd
          ls
